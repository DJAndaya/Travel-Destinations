import { createSlice } from "@reduxjs/toolkit"

const favoriteCountriesMapSlice = createSlice({
  name: "favoriteCountriesMap",
  initialState: [],
  reducers: {}
})

export default favoriteCountriesMapSlice